# NPM Configuration for BlogSpy SaaS
# This file handles peer dependency conflicts automatically

# Allow legacy peer dependencies (fixes React version conflicts)
legacy-peer-deps=true

# Optionally, you can also use:
# strict-peer-dependencies=false

# Auto yes for prompts (useful for CI/CD)
# yes=true

# Use exact versions when installing packages
# save-exact=true

# Logging level
loglevel=error

